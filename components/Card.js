'use client';import Link from 'next/link';import {motion} from 'motion/react';import {imageOf,titleOf} from '../lib/api';
export default function Card({item,type='anime',index=0}){const a=item?.attributes||{};return <motion.article className="card" initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.1}} transition={{delay:Math.min(index*.025,.2),duration:.4}} whileHover={{y:-7}}><Link href={`/${type}/${item.id}`}><div className="poster"><img src={imageOf(item)} alt={titleOf(item)} loading="lazy"/><div className="poster-shade"/><span className="card-index">{String(index+1).padStart(2,'0')}</span><span className="card-play">↗</span></div><div className="card-info"><h3>{titleOf(item)}</h3><div><span>{a.status||'Discover'}</span><span>{a.averageRating?`${Math.round(Number(a.averageRating))}%`:type==='anime'?'ANIME':'MANGA'}</span></div></div></Link></motion.article>}