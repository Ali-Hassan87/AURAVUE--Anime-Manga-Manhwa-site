'use client';import{useEffect,useState}from'react';import Link from'next/link';import Navbar from'../../components/Navbar';import Footer from'../../components/Footer';import Card from'../../components/Card';import{getCatalog}from'../../lib/api';export default function Manga(){const[items,setItems]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{getCatalog('trending','manga').then(x=>setItems(x.data||[])).finally(()=>setLoading(false))},[]);return <div className="site"><Navbar/><main className="hub"><div className="hub-head"><div><span className="eyebrow">MANGADEX / READING INDEX</span><h1>Manga & <i>manhwa.</i></h1><p>Discover titles from MangaDex, open full dossiers and jump into available English chapters.</p></div><div className="back-actions"><Link href="/">← Home</Link></div></div>{loading?<div className="loading-grid">{Array.from({length:8}).map((_,i)=><div className="skeleton" key={i}/>)}</div>:<div className="hub-grid">{items.map((x,i)=><Card key={x.id} item={x} type="manga" index={i}/>)}</div>}</main><Footer/></div>}